import * as ping from "./slash/utilities/ping";
import * as it_excuses from "./slash/common/it_excuses";
import * as reload_commands from "./slash/administration/reload_commands";

export const slashcommands = {
    ping,
    it_excuses,
    reload_commands,
};

export const commands = {

}